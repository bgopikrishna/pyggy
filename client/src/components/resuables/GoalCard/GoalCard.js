import React from 'react'

const GoalCard = ({ title, labels, progress, onFavourite, onEdit }) => {
    return (
        <div>
            <span>GoalCard</span>
        </div>
    )
}

export default GoalCard
