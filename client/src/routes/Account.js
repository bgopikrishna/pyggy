import React from 'react'
import { Skeleton } from 'antd'

const Account = () => {
    return (
        <div>
            <Skeleton active></Skeleton>
            <Skeleton active></Skeleton>

            <Skeleton active></Skeleton>

            <Skeleton active></Skeleton>

            <Skeleton active></Skeleton>
        </div>
    )
}

export default Account
